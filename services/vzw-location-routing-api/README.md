# VZW Location Routing API

This repo-shaped service represents the routing and geocoding capability exposed to Verizon partners in the demo.

It is modeled as a shared location platform service that provides route calculation and address resolution for dispatch workflows.
